class Unicorn < ApplicationRecord

    # validates :name, :tailcolor, presence: true

    # validates :tailcolor, numericality: true

    # validates :tailcolor, numericality: { only_integer: true }, allow_blank: true

    # validates(:name, presence: true,
    #           length: {minimum: 3, message: 'Must be at least 3 characters.'})

    # validates(:name, presence: true, length: {is: 3, message: 'Must be 3 characters long.'})

    # validates :name, uniqueness: true, presence: true

end
